<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/font-awesome/css/font-awesome.min.css" />
    <link rel="stylesheet" href="/styles/style.css" />
    <link rel="icon" type="imag3/x-icon" href="./assets/icon-dark.png" />
    <title>Authentication</title>
</head>
<body class="w-full h-[100vh] flex items-center justify-center flex-col gap-4">
    <% if(error) { %>
        <div id="message" class="flex flex-col gap-1 border p-3 border-red-600 bg-red-200 w-full max-w-[300px]">
            <div class="flex justify-between">            
                <h3 class="text-red-800 font-bold text-base">Error</h3>
                <button id="close_btn" class="flex items-center justify-center">
                    <i class="fa fa-plus rotate-45 text-red-600" aria-hidden="true"></i>
                </button>
            </div>
            <p class="text-red-600 text-sm"><%= message %></p>
        </div>
    <% } %>
    <form action="/auth" method="POST" class="flex flex-col gap-3 border border-black p-4 w-full max-w-[300px]">
        <img src="/assets/icon-light.png" alt="Logo" class="w-[25px] aspect-square mb-4" />
        <div class="flex flex-col mb-8">
            <h1 class="font-bold text-lg">Authentication</h1>
            <p class="mb-2 text-sm text-gray-600">Password needed to ensure you're authorized to do anything.</p>
        </div>
        <input type="password" name="password" placeholder="Password" class="border border-black h-[40px] px-3" />
        <button class="px-4 h-[40px] border border-black text-white bg-black hover:bg-transparent hover:text-black">Log in</button>
    </form>
</body>
<script>
    var message = document.getElementById('message');
    document.getElementById('close_btn').addEventListener('click', () => message.remove());
</script>
</html>